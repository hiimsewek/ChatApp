import { StyleSheet } from "react-native";

export default StyleSheet.create({
  chipsListContainer: {
    maxHeight: 40,
    flex: 1,
  },
  chipsListContentContainer: {
    height: 40,
    justifyContent: "center",
  },
});
