import { StyleSheet } from "react-native";

export default StyleSheet.create({
  messageDetailsContainer: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
  },

  timestampText: { fontSize: 12, marginRight: 15 },
});
