import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerAlignment: {
    flexDirection: "row",
    alignItems: "center",
  },
  chatPhotoSpacing: { marginRight: 10 },
});
