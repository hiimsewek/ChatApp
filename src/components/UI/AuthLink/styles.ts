import { StyleSheet } from "react-native";

export default StyleSheet.create({
  displayRow: {
    flexDirection: "row",
    alignItems: "center",
  },
  signupLink: {
    fontWeight: "bold",
    textDecorationLine: "underline",
  },
  buttonMargin: {
    marginHorizontal: 0,
  },
});
