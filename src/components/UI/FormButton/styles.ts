import { StyleSheet } from "react-native";

export default StyleSheet.create({
  buttonPosition: {
    marginVertical: 15,
    justifyContent: "center",
  },
  buttonSize: {
    height: 60,
    maxWidth: 500,
    width: "100%",
  },
});
